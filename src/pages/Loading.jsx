import React from 'react';

const Loading = () => {
    return (
        <div className='flex min-h-screen justify-center items-center'>
            <span className="loading mx-auto my-auto loading-spinner text-error"></span>
        </div>
    );
};

export default Loading;